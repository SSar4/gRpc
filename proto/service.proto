syntax = "proto3";

package mypackage;

// Definindo a mensagem
message MyMessage {
  string id = 1;
  string content = 2;
}

// Definindo o serviço
service MyService {
  // Método para enviar uma mensagem
  rpc SendMessage(MyMessage) returns (MyMessage) {}
}
